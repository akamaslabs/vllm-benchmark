# Studies index — recap

At-a-glance recap of every study: which folder, how it's configured, and what was
observed — "folder X holds study Y, configured like this, we observed Z". Full detail
(exact YAML, versions, raw data) lives in each study's own `README.md`; this table is
for scanning across studies without opening every folder.

This is a **factual recap**, not a plan — for what to try next, see `ROADMAP.md`
(section B is the forward-looking backlog; this table is the backward-looking record of
what's actually been built and found).

| Study folder | Status | Configured as | Observed |
|---|---|---|---|
| [0-explorative](0-explorative/README.md) | DONE | vLLM (16/26 params), Qwen2.5-7B-Instruct on 1x A10G, attention_backend/kv_cache_dtype/gpu_memory_utilization/max_num_seqs the levers that mattered | +12.5% throughput vs. baseline (`FLASHINFER`+`fp8_e4m3`+`block_size=32`), with lower latency and higher success rate too — not a trade-off |
| [1-goodput-realistic-load](1-goodput-realistic-load/README.md) | DONE | vLLM (14/30 params, `spec_method`/`spec_tokens` dropped after 5 crashes), same Qwen2.5-7B-Instruct/1x A10G as `0-explorative`; goal = goodput (throughput + P95 TTFT/ITL SLA); AIPerf + real ShareGPT + 7-level concurrency sweep | Stopped at 31 experiments (user); **best trial (exp 29) hit +45.5% goodput vs. baseline (3131 vs. 2152 tokens/s) while staying SLA-compliant** — `FLASHINFER`+`fp8`+`gpu_memory_utilization≈0.92`+`max_num_seqs≈770`; 5 independent top trials converged on the same region |
| [2-larger-model-g7e](2-larger-model-g7e/README.md) | TODO | vLLM (14/30 params, same as `1-goodput-realistic-load` minus `spec_method`/`spec_tokens` pinned off), `Qwen/Qwen3.8-27B` (dense, 27.8B) on 1x RTX PRO 6000 Blackwell 96GB (`g7e.4xlarge` — swapped 2026-08-21 from H100/`p5.4xlarge`, no regional capacity; reuses the `vllm-bench` cluster via a new node group); goal = goodput, same AIPerf/ShareGPT/concurrency-sweep methodology as `1-goodput-realistic-load` carried forward pending recalibration | — |
| [5-pack-changes](5-pack-changes/README.md) | TODO | Not a tuning study — verifies 23 new GPU/DCGM + 11 new Kubernetes/PSI pack metrics collect end-to-end. Cloned from `3-comparison-a10` (same vLLM/1x A10G stack), no goal constraints, `trim` windowing, single flat concurrency=428/5min load, ~10 experiments | — |
| [6-data-parallelism](6-data-parallelism/README.md) | TODO | Not a tuning study — 4 fixed configs (`baseline`+3 `preset` steps) isolating `vLLM.data_parallel_size` (1/2/3/4), every other parameter at vLLM's default. Same Qwen2.5-7B-Instruct/goal/windowing/load pattern as `2-larger-model-g7e`, on 1x g6.12xlarge (4x NVIDIA L4 24GB, no NVLink) — first study on this GPU generation | — |
| [8-parallelism-tuning](8-parallelism-tuning/README.md) | TODO | Goodput (same goal/SLA/windowing as `2-larger-model-g7e`) on 4x L4 (`7-tensor-parallelism`'s system/telemetry), tuning that study's 14 vLLM parameters **plus `tensor_parallel_size` and `data_parallel_size` [1,4]** with `TP x DP <= 4` and `TP != 3` constraints (7 valid topologies); baseline = vLLM defaults (TP1/DP1) with `gpu_memory_utilization` 0.90; optimize 1000 experiments / 200 failures. Telemetry carries two query fixes not yet applied elsewhere (`${percentile}` -> 0.95, PSI window `[2m]`). | — |
| [9-goodput-per-gpu](9-goodput-per-gpu/README.md) | TODO | Clone of `8-parallelism-tuning` (same 16 tuned vLLM parameters, TP/DP constraints, 4x L4 system/telemetry) with the goal divided by **`vLLM.active_gpus`** (GPUs with > 1 GiB DCGM framebuffer in use = TP x DP) — goodput per GPU actually used; also collects `vLLM.active_dp_engines` (distinct vLLM `engine` labels = DP). Also exports the full NVIDIA Run:ai GPU-profiling DCGM counter set (+3 NVLink fields, GPU pack **1.2.0**). Needs vLLM pack **1.8.0** and GPU pack **1.2.0**. | — |
| [10-gpt-oss-20b-goodput-per-gpu](10-gpt-oss-20b-goodput-per-gpu/README.md) | TODO | Same goodput-per-GPU goal/SLA/windowing/AIPerf sweep as `9-goodput-per-gpu` on the same 4x L4 node, but serving **`openai/gpt-oss-20b`** (MoE, MXFP4 experts, attention sinks) on **vLLM 0.29.0**: 14 tuned parameters (study 9's minus `attention_backend` — pinned `TRITON_ATTN`, the only sink-capable backend on SM 8.9 — `disable_cascade_attn`, `tokenizer_mode`; plus `enable_expert_parallel`), 5 `parameterConstraints` (`TP x DP <= 4`, `TP != 3`, batched >= seqs, EP only if TP x DP > 1, per-GPU memory fit), `gpu_memory_utilization` <= 0.90, restart guard fails crashed trials; scaffolded 2026-09-14, never run (no `g6.12xlarge` capacity) | — |
| [11-gpt-oss-20b-tps-peak](11-gpt-oss-20b-tps-peak/README.md) | TODO | **Short-ramp twin of `10-gpt-oss-20b-goodput-per-gpu`** (scaffolded 2026-09-15): same model (`openai/gpt-oss-20b`), same 4x L4 node, same goal/SLA and the same 14 parameters, domains and 5 `parameterConstraints` — the load profile is the only deliberate difference. AIPerf runs 4 log-spaced levels (128/256/512/1024, ratio 2x, ceiling matching `max_num_seqs`'s own [16, 1024] domain) at 90s each instead of 12 levels at 300s, so a trial costs ~6 min of load and an experiment ~11 min against ~75. `windowing.stability.resolution` pinned to `15s` so the 6-sample window is exactly one concurrency level; optimize 60/12 instead of 200/40 (~11 h, one overnight run). | |
| [12-gpt-oss-20b-steady-throughput](12-gpt-oss-20b-steady-throughput/README.md) | TODO | **Ramp dropped entirely** (scaffolded 2026-09-15): same model, node, goal/SLA and 14 parameters as studies 10/11, but AIPerf offers a single constant load of **512 concurrent requests for 600s** instead of a concurrency sweep. 512 is the measured discrimination point — one L4 at the default `max_num_seqs=256` queues from concurrency 302, while TP2/DP2 at `max_num_seqs=394` (capacity 788) is still queue-free at 512. One regime per trial means windowing becomes `trim: [2m, 30s]` on RunTest with no selection at all, removing both the inter-level gaps and the window-vs-level misalignment that cost study 11 ~10% on an identical baseline. 45 experiments, ~11.5 h. | |
| [13-gpt-oss-20b-tp-goodput-per-gpu](13-gpt-oss-20b-tp-goodput-per-gpu/README.md) | STOPPED | **Tensor-parallel-only twin of studies 10/12** (scaffolded 2026-09-15 from `12-gpt-oss-20b-steady-throughput`): same model (`openai/gpt-oss-20b`), 4x L4 node, goal/SLA and 14 parameters, but `tensor_parallel_size` in **[2, 4]** (TP != 3) and `data_parallel_size` in [1, 2], so every trial shards the model over >= 2 GPUs (TP2/DP1, TP2/DP2, TP4/DP1). Load and windowing go **back to study 10**: 12-level AIPerf ramp 150->1024 x 300 s and `stability` windowing on the prefill peak (~75 min per experiment, optimize 100/20 ~= 5 days). Adds a **sampler-warmup OOM guard** `gpu_memory_utilization * 22.03 + max_num_seqs * 0.00375 <= 21.63` (from study 12's experiment 12: CUDA OOM in `warmup_kernels` at 0.90/1024, 786 MiB fp32 logits copies over the 201k vocab, after weights + KV cache were allocated) and a crash-loop fail-fast in `apply_config.sh`. Baseline TP2 + vLLM defaults + `gpu_memory_utilization` 0.90. | Ran 2026-09-15/16, **stopped after 16 experiments** (of 100) to make room for study 14 — the stop is per study 14's manifest; the last state read live was RUNNING with 16 finished. Best exp. 16 = 2345.31 tokens/s per GPU (+18.49% vs. baseline 1979.41): TP2/DP1, EP on, `kv_cache_dtype` auto, `gpu_memory_utilization` 0.80, `max_num_seqs` 1024, `max_num_batched_tokens` 7781, block 96, optimization level 3. Its Sobol initial design **perfectly aliased** `kv_cache_dtype == fp8` with `enable_expert_parallel == false` across all 15 optimizer experiments, so neither effect is identifiable — the reason study 14 exists. All 16 experiments imported into study 14. |
| [14-gpt-oss-20b-parallelism-and-kv-goodput-per-gpu](14-gpt-oss-20b-parallelism-and-kv-goodput-per-gpu/README.md) | RUNNING | **Successor of `13-gpt-oss-20b-tp-goodput-per-gpu`** (created 2026-09-16): same model, node, goal, SLA, windowing, workflow and 14 parameters, and it **reuses study 13's system, telemetry instance and workflow** so the imported experiments stay comparable. Three changes: `tensor_parallel_size` reopened to **[1, 4]** and `data_parallel_size` to **[1, 4]**; study 13's baseline + experiments 2-16 imported via `baseline from` + `bootstrap`; and Sobol's initial design replaced by **10 explicit `preset` steps** with `numberOfInitExperiments: 0` — S3/S4/S10 break the fp8/expert-parallel alias, S6/S7 fill the TP1 cells study 13's domain excluded, S8/S9 isolate expert parallelism at TP2/DP2. 8 KPIs (two of which the reused telemetry instance does not collect: `total_token_throughput`, `request_queue_time_p95`). Optimize 100/20 after ~12.5 h of presets. | — |
| [15-qwen3-30b-a3b-parallelism-goodput-per-gpu](15-qwen3-30b-a3b-parallelism-goodput-per-gpu/README.md) | TODO | **First study whose model does not fit one GPU** (scaffolded 2026-09-17, phase 1 of the topology-then-NCCL plan agreed that day): `Qwen/Qwen3-30B-A3B-Instruct-2507-FP8` (MoE 128 experts/top-8, FP8 e4m3 block-quantized, 29.03 GiB) on the same 4x L4 `g6.12xlarge` node, same goodput-per-GPU goal/SLA/windowing and the same 14 parameters as study 14 — no bootstrap, no imported baseline (different model). The FP8 variant is deliberate: at 29.03 GiB single-GPU is impossible while 2-GPU (14.5 GiB/GPU) and 4-GPU (7.3 GiB/GPU) layouts are both loadable, so `active_gpus` really varies; the bf16 sibling (56.87 GiB) would force TP x DP = 4 everywhere and collapse the objective into raw goodput. 6 `parameterConstraints` — the memory-fit one excludes single-GPU by itself, and a new one requires expert parallelism at 4 ranks (`moe_intermediate_size` 768 / 4 = 192 is not a multiple of the checkpoint's 128-wide quantization block). Initial design = 10 explicit presets, `numberOfInitExperiments: 0`, covering all 5 reachable topologies and all 4 cells of kv_cache_dtype x expert-parallel. AIPerf ramp re-floored 150 -> 24 (KV is 96 KiB/token, 2x gpt-oss). | — |

## Maintaining this table

- **`/new-study`** adds a row when a study is scaffolded (`Configured as`/`Observed`
  left blank, `Status: TODO`).
- **`study-recap`** (invoked directly or via `/log-results`) fills in `Configured as`
  (one line: target component(s) + the 2-3 parameters that matter + hardware/stack in
  brief) and `Observed` (one line: the headline result) and flips `Status` to `DONE`,
  once a study finishes. Pull the exact wording from that study's own README rather than
  re-deriving it.
- Keep every cell to one line — link to `studies/<name>/README.md` for the rest. If a
  one-liner can't do a result justice, that's a sign the reader should follow the link,
  not a reason to expand this table.
